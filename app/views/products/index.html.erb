<div class="container mt-3">

  <p style="color: green"><%= notice %></p>
  <div class="row d-lg-none d-xl-none">
    <p>
      <a class="btn btn-primary collapseExample" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
        Categorias
      </a>

    </p>
    <div class="collapse" id="collapseExample">
      <div class="card card-body">
        Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. Nihil anim keffiyeh helvetica, craft beer labore wes anderson cred nesciunt sapiente ea proident.
      </div>
    </div>
  </div>
  <%= link_to "Nuevo producto", new_product_path, class:"btn btn-success btn-sm" %>

  <div class="row">
    <div class="col-12 col-lg-9">
      <h2>Products</h2>
      <div class="">

        <div id="products" class="row">
          <% @products.first(20).each do |product| %>
            <div class="col-12 col-md-12 col-lg-6">
              <div class="card p-5 m-3">
                <%= render product %>

                <%= line_chart product.prices.group(:updated_at).sum(:price), suffix: "$" %>
                <%= link_to "Ver producto", product, class:'btn btn-primary' %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-12 col-lg-3 d-none d-sm-block d-md-none d-lg-block ">
      <h2>Categorias</h2>
      <div class="row">
        <% @categories.each do |category| %>
        <div class="col-lg-12 mb-2">
          <%= link_to category.title, category_path(category), class:"attributes-size btn btn-sm btn-primary" %>
        </div>
        <% end %>
      </div>
    </div>
  </div>

</div>

<script>
  document.querySelector(".collapseExample").addEventListener("click", console.log("je"))
</script>